# gate-suites — the suites gate-spira.sh runs, and why each one earns the wait.
#
# NOTE: gate-spira.sh is NOT the production landing gate (sp-wyep). The landing gate is the
# fence CMD in the repo-map. This file names the suites that gate-spira.sh runs on a schedule;
# a red here files a bead but does not block landings.
#
# THIS IS THE ONLY HAND-WRITTEN LIST OF SUITES, and it is deliberately not a list of what
# EXISTS. What exists is a glob over `spira/test-*.sh`; this file names the subset that
# gate-spira.sh runs on the timed pass, which is a choice somebody has to make and cannot be
# derived. Everything the glob finds and this file does not name is run by `suites.sh` on a
# timer without gate-spira.sh — so the complement is derived, and a suite dropped from here
# moves to the timed run rather than falling out of the world.
#
# THE FAILURE THIS SHAPE EXISTS TO PREVENT. The list used to live inside gate-spira.sh, and
# nothing else could read it, so the timed runner could not know what the gate already
# covered and the only way to answer "what runs nowhere" was for a person to compare two
# things by eye. Nobody did, for as long as the arrangement lasted: five of nine suites ran
# nowhere at all, three of them landed the same night citing themselves as verification.
#
# A LINE HERE MUST NAME A FILE THAT EXISTS. gate-spira.sh refuses to report a pass when one
# does not, because a gate that quietly runs five checks where six are written down is a gate
# reporting on work it never looked at.
#
# A SUITE NAMED HERE IS FIXED IN THE COMMIT THAT BREAKS IT, OR DELETED WITH THE GAP RECORDED
# HERE — never left running and red with a note explaining why. Red-with-an-explanation is not
# a third option: it masks every future regression in the code it covers while costing a slice
# on every branch, and a stale note reads identically to a live one.
#
# Blank lines and `#` comments are ignored. One repository-relative path per line.

# THE VERDICT CACHE'S OWN, and it earns its place by being the one check between a branch and
# a landing that decides whether any of the others run. A cache that answers a question it was
# never asked lands work on a trial nobody performed, and it does it silently: a reused verdict
# and a real pass are the same exit status, in the same words, at the same cost as success. Five
# seconds, and it plants an offender for every side of the key.
spira/test-gate-verdict.sh

# THE HOST-ESCAPE FENCE'S OWN POSITIVE CONTROL. host-check.sh requires every suite to run
# in a container or carry a # host-reason: declaration; this suite plants an undeclared host
# suite (SEEN RED), withdraws it (SEEN GREEN), and verifies the count suites.sh renders.
# host-check.sh replaces both hermetic.sh's main scan and the fixture-contamination fence
# (both deleted in sp-1ctx): a suite without a declaration cannot be added silently, which
# is the class of defect the contamination fence existed to detect after the fact.
spira/test-host-reason.sh

# THE ATTEMPT LEDGER'S OWN. An attempt count decides whether work is poisoned, so a count
# that charges the harness for its own restarts removes work nobody ever tried.
spira/test-attempts.sh

# THIS GATE'S OWN YIELD METER, and it is the condition on the gate being here at all. A check
# may sit between work and its landings only while it is catching real defects
# (law-gate-earns-its-place); yield.sh is the count that makes that sentence actionable, and
# the specific way it fails is by silently ceasing to be called — which renders as a tidy "no
# gate faults" and reads as good news. The suite plants a real defect and a red that is a
# suite reading the box, and requires them in different columns.
spira/test-yield.sh

# THE SOP LINT FENCE'S OWN POSITIVE CONTROL. gate-spira.sh calls sop.sh lint as a fence to
# catch SOPs written directly with `bd remember` — the back door that bypasses the write
# validator. Without a positive control, a lint that passes an empty shelf and a lint that
# cannot detect anything look identical. This suite (which absorbed test-sop-lint.sh) plants
# a malformed shelf via the SOP_SHELF_CMD seam and requires lint to name it; the gate's own
# call into sop.sh lint is confirmed separately, at T0, by test-sop-gate-wired.sh.
spira/test-sop.sh
spira/test-sop-gate-wired.sh

# THE GATE'S OWN BUDGET ENFORCEMENT. gate-spira.sh times itself and files a bead when it
# overruns SPIRA_GATE_BUDGET; this suite proves the timing, the bead filing, and the dedup.
# In the timed suite run it consistently exceeds the per-suite slice (it invokes gate-spira.sh
# internally, whose own suites add up beyond the slice) and is killed as a false red.
# Moving it here ends that loop: gated suites run without a per-suite slice and this suite
# can reach its natural completion.

# THE LITERAL-LINT FENCE'S OWN POSITIVE CONTROL. A static check that reports a clean tree
# is indistinguishable from one whose matcher never fires, so this suite plants an offender
# in a scratch repository, requires the fence to name it, withdraws it, and only then reads
# the shipped tree's silence as evidence of a clean tree. Without this, the fence could fail
# to match for any reason and the gate would call it a pass.
spira/test-literal-lint.sh

# THE SCRATCH-FENCE'S OWN POSITIVE CONTROL. scratch-fence.sh refuses sp-* notes and
# *.fixed artefacts at the repository root — aeon working notes that ship to every consumer
# and cost suite time on every branch that follows them. This suite plants each pattern
# (SEEN RED), withdraws it (SEEN GREEN), and confirms the fence is referenced in gate-spira.sh.
# Without this, a fence whose matcher never fires is indistinguishable from one that passes
# a clean tree.
spira/test-scratch-fence.sh

# THE WIKI BLANKET-ADD FENCE'S OWN POSITIVE CONTROL. wiki-add-fence.sh refuses any
# non-comment code line that pairs SPIRA_WIKI with a blanket staging form (add -A, add .,
# commit -a). This suite plants each offender (SEEN RED), withdraws it (SEEN GREEN), and
# verifies that comment lines are not flagged, confirming the matcher fires on code only.
spira/test-wiki-add-fence.sh

# SCRATCH-FENCE AT CERTIFICATION: a root-level scratch file is refused before a CERTIFIED
# record is written. The cost of a fence violation escaping certification is a full CI run
# (image build + suites) blocked for every branch in the batch — sp-zmd3u-sop-verification.md
# demonstrated this in PR 59. The fence runs as part of the certification gate; this suite
# verifies that a branch with a root-level sp-* file is refused (gate called, bead reopened,
# file named in the note) and that a clean branch is still certified (positive control).
spira/test-cert-lint.sh

# THE SUITE-STATE FENCE'S OWN POSITIVE CONTROL (D4/UC-28). suite-state-fence.sh refuses a
# landing that carries a malformed lifecycle entry or a quarantine against a CLOSED bead
# (which has no exit path — hygiene requires LANDED, which CLOSED never satisfies).
# Formerly test-suite-state-fence.sh, a T2 suite whose structural rules duplicated the
# ones already run here against no real DB; that suite is gone and this file carries all
# of it, the CLOSED-bead rule demoted to T1 with a stub `bd show`. Whether gate-spira.sh
# is wired to call the fence is test-gate-fences.sh's row, reading gate_fence_list.
spira/test-suite-state.sh

# THE GATE'S OWN FENCE-WIRING LIST (D7). gate-fences.sh exposes gate_fence_list, read by
# gate-spira.sh's own fence loop; this suite asserts the list equals the expected set and
# that gate-spira.sh reads it rather than carrying a second, hand-written copy of the same
# names — replacing three suites that each grepped gate-spira.sh's source for their own
# fence's name.
spira/test-gate-fences.sh

# CHAMBER INTEGRITY'S OWN T0 STAGE (UC-33). Every {{PLACEHOLDER}} a chamber brief carries
# is rendered by its filler, every command a brief names exists and is executable, every
# fayth's brief clauses that have no other home are present, and `bd reopen` is called
# only through `bead_reopen`. Extracted from test-spike.sh, which paid for a Dolt fixture
# and a bare git remote to run these pure text reads.
spira/test-lint-chamber.sh

# THE PREFLIGHT CHECKS' OWN POSITIVE CONTROL, including the missing-cmd path (formerly
# test-gate-missing-cmd.sh, folded in by sp-ajxg3 per docs/test-plan/gate-verdict.md
# section 4 cluster #2). gate.sh resolves every bash <path> in the repo-map gate command
# against the base tree before running any trial; a path absent from the base returns a
# distinct config-error verdict rather than BASE_FAIL. This class of defect (gate command
# naming a file not yet on the base) blocked 22 branches with a misleading verdict
# (sp-lkzl). This suite plants an absent path (SEEN RED), requires the named file to
# appear in the output, and confirms the verdict is not BASE_FAIL. The positive control
# adds the file to the base and requires the gate to reach the suites.
spira/test-gate-preflight.sh

# THE LANDING REBASE LIFECYCLE'S OWN FIXTURE INITIALIZATION. test-landing-rebase.sh covers
# rebase_branch classification, the survivor sweep, the loop-defer guard, and checkout
# refresh — all critical harness mechanisms. Flipped off server-mode testdb (sp-w92w1: it
# never reads through bd sql), which cut it from 240s to ~36s; kept here rather than moved
# to the timed set because the mechanisms it covers are still critical-path.
spira/test-landing-rebase.sh

# THE DEPS-LINT FENCE'S OWN POSITIVE CONTROL. deps-lint.sh refuses any external program
# invoked in spira/*.sh or Rust sources that is not declared in deps.toml; this suite
# plants an undeclared program (SEEN RED), withdraws it (SEEN GREEN), and confirms the
# lint finds the shipped tree clean. Without a positive control, a lint that never fires
# is indistinguishable from one that passes a clean tree.
spira/test-deps-lint.sh

# THE BUILD FENCE'S OWN POSITIVE CONTROL. build-fence.sh runs `make build` under the
# pinned toolchain whenever a diff touches Cargo.toml, Cargo.lock, a crate's src/,
# rust-toolchain, or the Makefile — the check fences-only certification (SPIRA_GATE_SUITES=off)
# used to skip entirely, which let sp-upkae certify green and then break the whole batch's
# build job (sp-9uro3). This suite plants a real, deliberately uncompilable crate (SEEN RED),
# fixes it (SEEN GREEN), and confirms build-fence.sh is wired into gate-touched.sh so it runs
# regardless of SPIRA_GATE_SUITES.
spira/test-build-fence.sh

# THE LOCKFILE LINT'S OWN POSITIVE CONTROL. lockfile-lint.sh refuses a Cargo.lock version
# bump with no matching Cargo.toml change — the local host's newer cargo can re-resolve a
# registry package to a version the gate's pinned toolchain cannot parse with nothing to
# review in the diff. This suite plants such a bump in a scratch git repository (SEEN
# RED), gives it a matching Cargo.toml change (SEEN GREEN), and confirms lockfile-lint.sh
# is wired into gate-touched.sh alongside build-fence.sh so it runs regardless of
# SPIRA_GATE_SUITES.
spira/test-lockfile-lint.sh

# THE TMUX-SCOPE FENCE'S OWN POSITIVE CONTROL. tmux-scope-fence.sh refuses a suite that
# calls a tmux session command, or runs concierge.sh start/wake/here/stop, without scoping
# it to a socket of its own — the class that reached the operator's real tmux server twice
# (sp-pfca0: test-concierge.sh run directly killed the cockpit, then OOM-killed itself).
# This suite plants an unscoped offender of each kind (SEEN RED), withdraws it (SEEN
# GREEN), confirms -L/TMUX_TMPDIR/CONCIERGE_SOCKET scoping clears the same shape, and
# requires the fence to find nothing in the shipped tree.
spira/test-tmux-scope-fence.sh

# NOT IN THE GATE (budget): test-watchtower, test-soak, test-owned, test-uninstall, test-ready,
# test-install-conflicts, test-configure. test-watchtower (~263s) was moved to the
# timed set to bring the gate under SPIRA_GATE_BUDGET (sp-6ceo); the pipeline-stall detection
# it covers is still caught on the ~1h timed-run window. test-soak (~90s) was moved to the
# timed set earlier (sp-jb7y); the deadlock it covers is still caught on the ~1h timed-run
# window. The remaining suites cover install/uninstall/configure/ready/owned/build paths;
# regressions in those paths are caught within one timed-run window, not at landing. Promote
# one when the budget allows.
