[workspace]
members = [
    "loom",
    "cockpit/panel",
    "broker",
    "czar-pass",
    "supervise",
    "spira-config",
    "queue-watch",
    "tsd",
    "desired-state",
    "batcher",
    "batcher-cut",
    "tsd",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
