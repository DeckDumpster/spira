[package]
name = "czar-pass"
version = "1.0.0"
edition = "2021"
description = "Czar fast pass — deterministic queue-stall detection on a 30-second timer."

[dependencies]
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
