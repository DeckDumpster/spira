//! batcher — pure core only (sp-xr8rf). The IO seam that drives it with git, testenv-batch,
//! the forge and the bead store, and wires it into the queue in place of batch.sh's cut, is
//! a separate crate (sp-jzfog).

pub mod core;
